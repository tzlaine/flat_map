struct uniquely_sorted_t { unspecified };
inline constexpr uniquely_sorted_t uniquely_sorted{ unspecified };
using value_type = pair<Key, T>;

Fix sequence container mistake in wording.

Poll: We want 1 allocator (not 2). 7/4/2/0/0 [DONE]
Poll: We are OK with sink-by-value. 3/4/2/1/1 [DONE]
Poll: We want separate split_flat_map and flat_map. 0/0/2/1/9 [N/A]
Poll: There is only a single flat map - call it split_flat_map. 0/1/0/3/10 [N/A]
Poll: Change the return type of extract to a struct. 6/6/2/3/0 [DONE]
Poll: Change all uses of pair to structs. 1/0/3/5/5 [N/A]
